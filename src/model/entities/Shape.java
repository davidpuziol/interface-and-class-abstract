package model.entities;
/*
 * Todos os que implmentam essa interface tem que implementar area()
 * Por padrao o metodo e public e abstract dentro de uma interface, nao sendo necessario declarar
 */

public interface Shape {

	double area();
}
